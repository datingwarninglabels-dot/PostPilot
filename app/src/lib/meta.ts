/**
 * The Meta Graph API version every Facebook/Instagram call uses. Meta deprecates
 * a version roughly 2 years after release (v21.0 shipped Oct 2024), so this is the
 * one string to bump — check https://developers.facebook.com/docs/graph-api/changelog
 * when a call starts returning a deprecation warning.
 */
export const GRAPH_VERSION = "v21.0";
